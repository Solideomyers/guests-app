# Backend Environment Variables for Railway Deployment

# Node Environment
NODE_ENV=production

# Server Configuration
PORT=3000
API_PREFIX=api
API_VERSION=v1

# Frontend URL (Your Vercel deployment)
FRONTEND_URL=https://your-app.vercel.app

# Database (PostgreSQL)
# Railway will auto-generate this if you add PostgreSQL service
# OR use your Neon database connection string
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# Redis Cache
# Railway will auto-generate these if you add Redis service
REDIS_HOST=redis.railway.internal
REDIS_PORT=6379

# Optional: Redis password (if secured)
# REDIS_PASSWORD=your-redis-password

# Notes:
# 1. Do NOT commit this file with real values
# 2. Set these variables in Railway dashboard or via CLI
# 3. DATABASE_URL must include ?sslmode=require for Neon
# 4. FRONTEND_URL should match your Vercel deployment URL
