# Render + Vercel Backend Configuration
# Production Environment Variables Template

# Database (Neon PostgreSQL - Recommended)
# Get from: https://neon.tech (Free tier: 0.5GB, always-on)
DATABASE_URL=postgresql://user:password@host-region.neon.tech:5432/database?sslmode=require

# Redis Cache (Upstash Redis - Required)
# Get from: https://upstash.com (Free tier: 10K commands/day)
# IMPORTANT: Use ONLY the hostname (no https://, no rediss://)
REDIS_HOST=your-redis-name.upstash.io
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password
REDIS_TLS=true
REDIS_DB=0

# Cache Configuration
CACHE_TTL=300

# Application Settings
NODE_ENV=production
PORT=10000

# API Configuration
API_PREFIX=api
API_VERSION=v1

# CORS - Add your Vercel frontend URL
FRONTEND_URL=https://your-app.vercel.app

# Optional: Monitoring & Analytics
SENTRY_DSN=
